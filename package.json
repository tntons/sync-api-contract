{
  "name": "@tntons/sync-api-contract",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that propagates a shared API contract into consumer repositories as vendor/ pull requests.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}